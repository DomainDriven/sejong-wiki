wiki:
  workingDirectory: ${user.dir}/src/test/resources/wikifile
  gitUrl: doNothing
  # TODO ignoreFiles는 리스트를 사용할 수 있도록 변경하기.
  ignoreFiles: .DS_Store|_attachements|.git|.gitignore

spring:
  profiles.active: unitTest